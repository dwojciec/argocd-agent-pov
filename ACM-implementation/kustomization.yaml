# GitOpsCluster / Placement / ManagedClusterSetBinding manifests are templates under
# ACM-implementation/*.yaml.template — render them with:
#   ./ACM-implementation/scripts/apply-gitopsclusters.sh
# Do not use `oc apply -k ACM-implementation` for those resources.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
