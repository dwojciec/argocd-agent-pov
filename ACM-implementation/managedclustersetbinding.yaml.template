# Exposes ManagedClusterSet `${CLUSTER_SET_NAME}` into `${GITOPS_NAMESPACE}` so a Placement
# in that namespace can select clusters that belong to the set.
# metadata.name must match spec.clusterSet (ACM convention).
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSetBinding
metadata:
  name: ${CLUSTER_SET_NAME}
  namespace: ${GITOPS_NAMESPACE}
spec:
  clusterSet: ${CLUSTER_SET_NAME}
