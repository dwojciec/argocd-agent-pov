apiVersion: v1
kind: Service
metadata:
  name: {{ include "openshift-demo.fullname" . }}
  labels:
    {{- include "openshift-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "openshift-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
